import math
n = math.radians(float(input("DIGITE O ÂNGULO QUE VOCÊ DESEJA: ")))
print(f"""================================
SENO = {math.sin(n):.2f}
COS = {math.cos(n):.2f}
TAG = {math.tan(n):.2f}
================================""")